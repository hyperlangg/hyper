name: Compiler / compile path
description: Lowering, codegen, AOT, or --emit-exe issue (not general language design).
title: "[Compiler]: "
labels:
  - compiler
body:
  - type: markdown
    attributes:
      value: |
        For compile-path bugs: include **`hyper run`** and **`hyper compile`** / **`--emit-exe`** results if they differ.
        See [known limitations](doc/compiler/known-limitations.md).

  - type: dropdown
    id: compile_mode
    attributes:
      label: Compile mode
      options:
        - AOT run (`hyper run` / `hyper compile`)
        - Native binary (`hyper compile --emit-exe`)
        - both
    validations:
      required: true

  - type: input
    id: version
    attributes:
      label: Hyper version or commit
      placeholder: "release tag or commit SHA"
    validations:
      required: true

  - type: dropdown
    id: os
    attributes:
      label: Operating system
      options:
        - Linux
        - macOS
        - Windows
        - other
    validations:
      required: true

  - type: textarea
    id: reproducer
    attributes:
      label: Minimal `.hyp` reproducer
      placeholder: |
        with open("f.txt", "w") as f:
            f.write("x")
      render: shell
    validations:
      required: true

  - type: textarea
    id: compile_output
    attributes:
      label: Compile output
      description: Full error from `hyper compile` or link step for `--emit-exe`.
      render: shell
    validations:
      required: true

  - type: textarea
    id: run_output
    attributes:
      label: Interpreter output (if different)
      description: Leave empty if `hyper run` is not applicable or matches compile.
      render: shell

  - type: textarea
    id: expected
    attributes:
      label: Expected compile behavior
    validations:
      required: true
